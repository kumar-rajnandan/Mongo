{

  # Remove third or more than third time data and keep less than or equal to 2 or unique data
  counts = df.groupby(['edge_id', 'day_id', 'time_id', 'speed']).cumcount()+1
  df2 = df[counts <= 2]
  print(df2)
}
